import 'package:flutter/material.dart';

class OtoparkScreen extends StatelessWidget {
  static const routeName = '/otopark';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Otopark'),
      ),
      body: Center(
        child: Text('Otopark'),
      ),
    );
  }
}
